import { Component } from "@angular/core";

// NOTE - Component is Decorater
@Component({
    selector: 'app-header', //NOTE -  Unique selector to avoid collisions
    templateUrl: './header.component.html'
})
export class HeaderComponent {

}